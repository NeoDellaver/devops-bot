## 🚀 Для обновления бота на сервере выполните:

### Способ 1: Автоматический скрипт (рекомендуется)

```bash
cd /root/devops-bot
chmod +x update-bot.sh
./update-bot.sh
```

### Способ 2: Ручное обновление

```bash
cd /root/devops-bot

# Сохранить локальные изменения
git stash

# Получить обновления
git pull origin master

# Установить зависимости (выберите один вариант)

# Вариант A: Через venv (если существует)
source venv/bin/activate
pip install -r requirements.txt

# Вариант B: Если venv не существует
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Вариант C: Через python3 -m pip
python3 -m pip install -r requirements.txt

# Перезагрузить сервис
sudo systemctl restart devops-bot.service

# Проверить статус
sudo systemctl status devops-bot.service
```

## 🔍 Проверка работы

```bash
# Посмотреть логи (последние 50 строк)
sudo journalctl -u devops-bot.service -n 50

# Отслеживать логи в реальном времени
sudo journalctl -u devops-bot.service -f

# Проверить, что процесс запущен
ps aux | grep bot.py
```

## 📋 Что было изменено

**Исправлены ошибки из журнала:**
- ✅ TelegramNetworkError: ServerDisconnect
- ✅ Ошибка в back_to_modules
- ✅ Кнопки не реагируют на старте

**Улучшения:**
- Восстановлена полная обработка ошибок
- Добавлены таймауты для HTTP запросов
- Фиксированы дубликаты в requirements.txt
- Все обработчики теперь безопасны (try/except)
